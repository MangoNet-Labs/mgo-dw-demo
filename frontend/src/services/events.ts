import { createEvent } from 'effector'

export const clientSideRenderringEvent = createEvent("clientSideRenderringEvent")
export const clientSideRenderredEvent = createEvent("clientSideRenderredEvent")

export const signoutEvent = createEvent("signoutEvent")
